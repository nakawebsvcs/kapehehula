---

---

<!-- Bootstrap Carousel Component -->
<section class="carousel-section">
  <div class="text-center py-3 mt-5 mb-5">
  <h2 class="cs-title cinzel">Want to learn Hula?</h2>
  </div>
  <div id="imageCarousel" class="carousel slide" data-bs-ride="carousel">
    <!-- Indicators/dots -->
    <div class="carousel-indicators">
      <button type="button" data-bs-target="#imageCarousel" data-bs-slide-to="0" class="active" aria-current="true" aria-label="Slide 1"></button>
      <button type="button" data-bs-target="#imageCarousel" data-bs-slide-to="1" aria-label="Slide 2"></button>
    </div>

    <!-- The slideshow/carousel with your specified images -->
    <div class="carousel-inner">
      <div class="carousel-item active">
        <img src="/assets/images/kukuipuka-1.jpg" class="d-block w-100" alt="First slide">
      </div>
      <div class="carousel-item">
        <img src="/assets/images/kukuipuka-2.jpg" class="d-block w-100" alt="Second slide">
      </div>
    </div>

    <!-- Left and right controls/icons -->
    <button class="carousel-control-prev" type="button" data-bs-target="#imageCarousel" data-bs-slide="prev">
      <span class="carousel-control-prev-icon" aria-hidden="true"></span>
      <span class="visually-hidden">Previous</span>
    </button>
    <button class="carousel-control-next" type="button" data-bs-target="#imageCarousel" data-bs-slide="next">
      <span class="carousel-control-next-icon" aria-hidden="true"></span>
      <span class="visually-hidden">Next</span>
    </button>
  </div>
   <div class="col-sm-8 text-center" style="width: 100%; padding: 20px 40px;">
            <p style="max-width: 960px; margin: 3rem auto;">Classes are available from beginner to experienced. Located in Kahului, our Halau offers weekly classes in the styles of Kahiko and Auana. We hope you will join us!</p>
                </div> 
                </div>  
                <div class="container text-center">  
                <img class="lehua-div" src="/assets/images/lehua-removebg-preview.png" width="4%">
                <h1 class="cinzel mt-4">"I mohala no ke lehua i ke ke`ekehi `ia e ka ua."</h1>
<p class="mt-3"><em>Translation: The Lehua blossom unfolds when the rains tread it.<br>
Explanation: People respond better to gentle words than to scoldings.</em></p></div>
       <div class="text-center mt-4 register">
                    <a class="cs-button-image mt-4" href="https://docs.google.com/forms/d/e/1FAIpQLSfCd_hBIUYni5AiFq9AGUvtzn0Y79q0854gDARJYW-NvXXGgA/viewform" target="_blank">
                        Register for Classes
                    </a>
                </div>
    </div>
</section>

<!-- Add Bootstrap CSS and JS via CDN in the component -->
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM" crossorigin="anonymous">

<script>
  // Load Bootstrap JS once on initial page load
  if (typeof window !== 'undefined' && !window.bootstrap) {
    const bootstrapScript = document.createElement('script');
    bootstrapScript.src = 'https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js';
    bootstrapScript.integrity = 'sha384-geWF76RCwLtnZ8qwWowPQNguL3RmwHVBC9FhGdlKrxdiJJigb/j/68SIy3Te4Bkz';
    bootstrapScript.crossOrigin = 'anonymous';
    document.head.appendChild(bootstrapScript);
  }

  // Function to initialize the carousel
  function initCarousel() {
    const carouselElement = document.getElementById('imageCarousel');
    if (!carouselElement) return;
    
    // Check if Bootstrap is loaded
    if (typeof bootstrap !== 'undefined') {
      // Destroy existing carousel instance if it exists
      const existingCarousel = bootstrap.Carousel.getInstance(carouselElement);
      if (existingCarousel) {
        existingCarousel.dispose();
      }
      
      // Create new carousel instance with autoplay
      const carousel = new bootstrap.Carousel(carouselElement, {
        interval: 4500,
        ride: true,
        wrap: true
      });
      
      // Force start the carousel cycle
      setTimeout(() => {
        carousel.cycle();
      }, 100);
    } else {
      // If Bootstrap isn't loaded yet, wait and try again
      setTimeout(initCarousel, 200);
    }
  }

  // Initialize on first load
  document.addEventListener('DOMContentLoaded', initCarousel);
  
  // Re-initialize on Astro page transitions
  document.addEventListener('astro:page-load', initCarousel);
  
  // Clean up on page unload to prevent memory leaks
  document.addEventListener('astro:before-swap', () => {
    const carouselElement = document.getElementById('imageCarousel');
    if (carouselElement && typeof bootstrap !== 'undefined') {
      const existingCarousel = bootstrap.Carousel.getInstance(carouselElement);
      if (existingCarousel) {
        existingCarousel.dispose();
      }
    }
  });
</script>

<style>

    html body, 
html body p, 
html body .cs-text {
  font-family: "Afacad", Arial, sans-serif !important;
  font-size: clamp(1.4rem, 1.8vw, 2.3rem) !important;
}

   .cinzel {
    font-family: "Cinzel", "Arial", sans-serif;
  }

   h2 {
          font-weight: 400;
          font-size: clamp(1.8rem, 4vw, 2.5rem);
          margin-bottom: 2rem;
        }

        h1 {
          font-weight: 500;
          font-size: clamp(1.5rem, 2vw, 2rem);
        }

    .carousel-section {
    padding: clamp(3.75rem, 7.82vw, 6.25rem) 1rem;
  }
  /* Custom styles for the carousel */
  .carousel {
    width: 95%; /* Almost full screen width (95%) */
    margin: 0 auto;
    overflow: hidden;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
  }
  
  .carousel-item {
    height: 70vh; /* 70% of viewport height for a nice balance */
    transition: transform 1.5s ease-in-out;
  }
  
  .carousel-item img {
    object-fit: cover;
    height: 100%;
    width: 100%;
  }
  
  /* Make indicators more visible */
  .carousel-indicators button {
    width: 12px;
    height: 12px;
    border-radius: 50%;
    margin: 0 5px;
  }
  
  /* Style the control arrows */
  .carousel-control-prev, .carousel-control-next {
    width: 5%; /* Smaller controls for a cleaner look */
    opacity: 0.7;
    background-color: rgba(0, 0, 0, 0.2);
    border-radius: 0;
  }
  
  .carousel-control-prev:hover, .carousel-control-next:hover {
    opacity: 1;
    background-color: rgba(0, 0, 0, 0.3);
  }
  
  /* Responsive adjustments */
  @media (max-width: 768px) {
    .carousel-item {
      height: 50vh; /* Smaller height on mobile */
    }
    
    .carousel {
      width: 100%; /* Full width on mobile */
    }
  }

  section {
  .col-sm-8 {
    padding: 40px;
    
    p {
      margin-bottom: 40px;
    }
  }
  
  .container {
    margin-bottom: 40px;
    
    .lehua-div {
      margin-top: 2rem;
      margin-bottom: 2rem;
    }
    
    h1 {
      margin-bottom: 30px;
    }
    
    p {
      margin-bottom: 40px;
    }
  }
  
  .register {
    margin-top: 50px;
    margin-bottom: 50px;
  }
}

   /* Button */

  .cs-button-image {
  display: inline-block;
  padding: 1rem 2rem;
  font-size: 1.25rem;
  font-weight: bold;
  text-align: center;
  text-decoration: none;
  color: #ffffff; /* Text color */
  background-image: url('/assets/images/buttonbg.png'); /* Path to your image */
  background-size: cover; /* Cover the entire button */
  background-position: center; /* Center the image */
  border-radius: 0.5rem; /* Rounded corners */
  box-shadow: 0 0.5rem 1rem rgba(0, 0, 0, 0.15); /* Shadow effect */
  transition: transform 0.3s, box-shadow 0.3s;

  
  &::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-color: rgba(0, 0, 0, 0.5); /* Dark overlay */
    border-radius: 0.5rem;
    z-index: -1;
  }
  
  &:hover {
    transform: translateY(-3px);
    box-shadow: 0 0.75rem 1.5rem rgba(0, 0, 0, 0.2);
  }
  
  &:active {
    transform: translateY(-1px);
    box-shadow: 0 0.5rem 1rem rgba(0, 0, 0, 0.15);
  }
}

</style>